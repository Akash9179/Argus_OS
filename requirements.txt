# World model server and simulated vehicle.
# The contract itself installs from ./link (see the setup commands in CLAUDE.md).
fastapi>=0.115
uvicorn[standard]>=0.32
redis>=5.2
paho-mqtt>=2.1
protobuf>=5.28
python-ulid>=3.0
pyyaml>=6.0

# Tests
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.28
