# Buf module configuration for the LINK contract package.
# Lint keeps the schema clean; breaking-change detection enforces the
# contract freeze (link_version 1) once tagged.
version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
breaking:
  use:
    - FILE
